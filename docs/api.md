# API

The real-record-only test release exposes:

- `GET /api/public-data` for the normalized official snapshot plus current SEC
  filing metadata when configured
- `GET /health` for service health
- `GET /ready` for runtime capability status

The former fictional people, events, rankings, matches, regional intelligence,
workspace, and demonstration-key endpoints are not deployed.
